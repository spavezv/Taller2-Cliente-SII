<h1>Listing servicios</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Id empresa</th>
      <th>Creacion</th>
      <th>Precio</th>
      <th>Pagado</th>
      <th>Avance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @servicios.each do |servicio| %>
      <tr>
        <td><%= servicio.nombre %></td>
        <td><%= servicio.id_empresa %></td>
        <td><%= servicio.creacion %></td>
        <td><%= servicio.precio %></td>
        <td><%= servicio.pagado %></td>
        <td><%= servicio.avance %></td>
        <td><%= link_to 'Show', servicio %></td>
        <td><%= link_to 'Edit', edit_servicio_path(servicio) %></td>
        <td><%= link_to 'Destroy', servicio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Servicio', new_servicio_path %>
